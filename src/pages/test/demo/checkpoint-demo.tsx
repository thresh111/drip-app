import { Checkpoint } from '@/components/ai-elements/checkpoint';

function CheckpointDemo() {
  return <Checkpoint tooltip="Restores workspace and chat to this point" content="Restore checkpoint" />;
}

export default CheckpointDemo;
